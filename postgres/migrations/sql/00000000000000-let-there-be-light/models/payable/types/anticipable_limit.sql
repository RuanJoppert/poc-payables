CREATE TYPE anticipable_limit AS (
  maximum_amount           integer,
  maximum_anticipation_fee integer,
  maximum_fee              integer,
  minimum_amount           integer,
  minimum_anticipation_fee integer,
  minimum_fee              integer
);
